[project]
name = "wuzhiqi"
version = "0.1.0"
description = "AlphaZero-style Gomoku project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
